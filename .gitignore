node_modules/
.env
.env.*
!.env.example
!.env.production.example
dist/
coverage/
.next/
out/
outputs/
work/
*.log
.DS_Store
Thumbs.db
db/local-data/
scripts/*.png
*.tsbuildinfo
__pycache__/
*.pyc
.claude/

# Informes de auditoria: describen debilidades aun sin corregir y este repositorio
# es publico. Se mantienen fuera del control de versiones a proposito.
docs/auditoria/

# Salidas de trabajo local
output/

# Fotos y videos que sube la gente desde la aplicación: son evidencia de campo con datos de
# personas, y el primer `git add -A` de quien corra la app en local se las llevaría al repositorio.
apps/web/public/uploads/

# Pruebas locales con cuentas de contraseña conocida y datos sembrados. Estaba solo en
# .git/info/exclude, que no viaja: en otro clon del proyecto no protegía nada.
scripts/local/
